import { CarFilteringPipe } from './car-filtering.pipe';

describe('CarFilteringPipe', () => {
  it('create an instance', () => {
    const pipe = new CarFilteringPipe();
    expect(pipe).toBeTruthy();
  });
});
