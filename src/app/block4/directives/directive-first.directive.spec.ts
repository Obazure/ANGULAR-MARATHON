import { DirectiveFirstDirective } from './directive-first.directive';

describe('DirectiveFirstDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveFirstDirective();
    expect(directive).toBeTruthy();
  });
});
